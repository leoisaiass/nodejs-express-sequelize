const path = require("path"); // Path para configurar o caminho do arquivo de configuração do Sequelize

module.exports = {
  config: path.resolve("./src/database/config", "config.json"), // Caminho para o arquivo de configuração do Sequelize
  "models-path": path.resolve("./src/database/models"), // Caminho para a pasta de modelos do Sequelize
  "seeders-path": path.resolve("./src/database/seeders"), // Caminho para a pasta de seeders do Sequelize
  "migrations-path": path.resolve("./src/database/migrations"), // Caminho para a pasta de migrações do Sequelize
};
